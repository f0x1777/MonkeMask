{
  "name": "monkemask-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "pretest": "node -e \"const [a,b]=process.versions.node.split('.').map(Number); if(a<22||(a===22&&b<6)){console.error('npm test needs Node >=22.6 for --experimental-strip-types (have '+process.versions.node+')');process.exit(1)}\"",
    "test": "node --experimental-strip-types --test lib/transform-gestures.test.mts",
    "e2e": "playwright test"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "next": "15.5.18",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "22.10.7",
    "@types/react": "19.0.7",
    "typescript": "5.7.3"
  },
  "overrides": {
    "postcss": "^8.5.10"
  },
  "engines": {
    "node": ">=22.6.0"
  }
}
